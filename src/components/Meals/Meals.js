import MealsSummary from "./MealsSummary";
import MealsList from "./MealsList";

const Meals = (props) => {
    return(
        <>
            <MealsSummary/>
            <MealsList/>
        </>
    )
};

export default Meals
